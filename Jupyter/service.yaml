apiVersion: v1
kind: Service
metadata:
  name: jupyter-service
  labels: 
      app: jupyter-notebook
spec:
  type: NodePort
  selector:
    app: jupyter-notebook
  ports:
  - protocol: TCP
    nodePort: 32151
    port: 8888
    targetPort: 8888
 
